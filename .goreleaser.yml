before:
  hooks:
builds:
  - env:
      - CGO_ENABLED=0
    mod_timestamp: '{{ .CommitTimestamp }}'
    flags:
      - -trimpath
    ldflags:
      - '-s -w -X main.version={{.Version}} -X main.commit={{.Commit}}'
    goos:
      - linux
    goarch:
      - amd64
    binary: 'bootstrap'
    id: collector
    dir: collector
archives:
  - format: zip
    name_template: '{{ .ProjectName }}_collector_{{ .Version }}_{{ .Os }}_{{ .Arch }}'
    id: collector
    builds:
      - collector
release:
changelog:
  skip: true